name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master, 'claude/prepare-release-*']
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write  # push version bump commit, git tags, gh-pages branch, create releases

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Bump version
        id: bump
        run: |
          BUMP_TYPE="${{ github.event.inputs.version_bump || 'patch' }}"
          CURRENT=$(node -p "require('./package.json').version")
          IFS='.' read -r major minor patch <<< "$CURRENT"

          case "$BUMP_TYPE" in
            major) NEW_VERSION="$((major + 1)).0.0" ;;
            minor) NEW_VERSION="$major.$((minor + 1)).0" ;;
            patch) NEW_VERSION="$major.$minor.$((patch + 1))" ;;
          esac

          echo "new_version=$NEW_VERSION" >> "$GITHUB_OUTPUT"

          npm version "$NEW_VERSION" --no-git-tag-version

          DATE=$(date +%Y-%m-%d)
          TEMP=$(mktemp)
          awk -v ver="$NEW_VERSION" -v date="$DATE" '
            /^## \[Unreleased\]/ {
              print
              print ""
              print "---"
              print ""
              print "## [" ver "] - " date
              print ""
              print "### Changed"
              print "- Deployed version " ver
              next
            }
            { print }
          ' CHANGELOG.md > "$TEMP" && mv "$TEMP" CHANGELOG.md

          echo "Bumped $CURRENT -> $NEW_VERSION"

      - name: Commit version bump and push tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json CHANGELOG.md
          git commit -m "chore: bump version to ${{ steps.bump.outputs.new_version }} [skip ci]"
          git tag "v${{ steps.bump.outputs.new_version }}"
          git push
          git push origin "v${{ steps.bump.outputs.new_version }}"

      - name: Build
        run: npm run build
        env:
          GITHUB_PAGES: true

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.bump.outputs.new_version }}"
          NOTES=$(awk "/^## \[$VERSION\]/{found=1; next} found && /^## \[/{exit} found && NF{print}" CHANGELOG.md)
          gh release create "v$VERSION" \
            --title "v$VERSION" \
            --notes "${NOTES:-Released version $VERSION}" \
            --latest

      # In repo Settings → Pages, set source: "Deploy from a branch" → gh-pages / root
      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          commit_message: "deploy: v${{ steps.bump.outputs.new_version }}"
